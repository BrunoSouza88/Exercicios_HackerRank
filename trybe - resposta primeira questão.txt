var numero = 1;
var outroNumero = 2;
var operacao = "+";

function calculadoraAdicaoSubtracao(numero, outroNumero, operacao) {
    if(operacao == "+") 
    return numero + outroNumero
else if (operacao == "-")
    return numero - outroNumero
else
return 0

 
if(operacao == "+") 
    return numero + outroNumero
else if (operacao == "-")
    return numero - outroNumero
else
return "0"
}